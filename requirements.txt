# Core dependencies (compatible with Python 3.14)
opencv-python>=4.8.0
numpy>=2.0,<2.3.0

# Face recognition with deep learning
face-recognition>=1.3.0
# Use pre-built dlib wheel to avoid needing Visual Studio
dlib==19.24.0


# Object tracking
scipy>=1.10.0

# Optional (if face-recognition fails)
keras>=3.0.0
tensorflow>=2.15.0
scikit-learn>=1.3.0

# Object detection (will add in Phase 4)
# tensorflow
# torch
# torchvision

# Utilities
pillow>=10.0.0
